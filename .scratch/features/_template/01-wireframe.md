# 01 — Wireframe · <feature-slug>

Low-fidelity ASCII wireframes for **every screen and state**. Stay in the "Warm Cards"
language and the 3-tab shell. No code.

## Screen: <name> — default state
```
┌─────────────────────────────┐
│                             │
│   (sketch the layout here)  │
│                             │
└─────────────────────────────┘
```
Controls:
- `<button>` → does X

## Screen: <name> — empty state
```
```

## Screen: <name> — error / edge state
```
```

## Flow
<!-- entry point → screens → exit. Where does this hang off the existing app? -->

---
Gate 1: show these via AskUserQuestion (one variant per preview) and loop until the user
agrees on the UX. Then advance to the tech spec.
